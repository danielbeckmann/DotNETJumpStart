<Page
    x:Class="ImageApp.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImageApp"
    xmlns:vm="using:ImageApp.ViewModels"
    xmlns:common="using:ImageApp.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:LoginViewModel />
    </Page.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="24,12,0,0">
            <Image Source="/Assets/Image.png" />
            <TextBlock Text="Registrieren" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>
      
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="24,0">
            <TextBlock Text="Bitte wähle einen Benutzernamen" Style="{StaticResource BaseTextBlockStyle}"/>

            <TextBox Margin="0,12" Text="{Binding UserName, Mode=TwoWay}"/>
            
            <Button Content="Registrieren" Click="RegisterButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
